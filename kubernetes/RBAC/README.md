This script is used to create
  * namespace
  * service account
  * token
  * role
  * rolebinding
  * config
to limit this config only has full access to sepcify namespace.

Usage:
  xxx.sh NAMESPACE
